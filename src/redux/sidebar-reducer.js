let initialState = {
 userItems: [
				{id: 1, name: "Ihor"},
				{id: 2, name: "Zenuk"},
				{id: 3, name: "Tolik"}
			]
}

const sidebarReducer = (state = initialState, action) => {
 
 return state;
}

export default sidebarReducer;